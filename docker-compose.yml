version: "3"

services:
  tunnelserver:
    build:
      context: .
    environment:
      - DEBUG=*
    container_name: "tunnelserver"
    network_mode: "host"
    command: "--config config.json"
    restart: "always"
